#include "main.h"
/**
 * _isalpha - Entry point
 * @c: single letter
 * Return: always 1 if lower or upper case or 0 if otherwise
 */
int _isalpha(int c)
{
	if (i >= 'a' && i <= 'z' && i >= 'A' && i <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

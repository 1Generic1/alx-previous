#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 on Success
 */
void print_alphabet(void)
{
	char i = 'a'

		for (i = 'a'; i <= 'z'; i++)
		{
			_putchar(i);
		}
	_putchar('\n');
	return (0);
}

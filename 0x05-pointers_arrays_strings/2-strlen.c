#include "main.h"
/**
 * int _strlen - entry poitn of function that returns the length of a string
 * @s: the string
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int l;
	for (l = 0; s[l] != '\0'; l++)
	{
		;
	}
		return (l);
}

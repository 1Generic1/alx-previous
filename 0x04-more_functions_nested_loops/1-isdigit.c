#include "main.h"
#include <stdio>

/**
 * _isdigit - Entry point
 * @c: character to check
 * Return: 0 or 1 otherwise
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (0);
	}
	else
	{
		return (0);
	}
}

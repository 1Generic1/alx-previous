#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: always 0 on success
 *
 */
int main(void)
{
	int i = 'a';
		for (i = 'a'; i <= 'z'; i++)
		{
			putchar(i);
		}
		putchar('\n');
	return (0);
			}

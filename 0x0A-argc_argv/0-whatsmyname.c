#include <stdio.h>

/**
 * main - entry point
 * @argc: number of command argument in the program
 * @argv: array of command line value
 *
 * Return: Always 0 on success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
